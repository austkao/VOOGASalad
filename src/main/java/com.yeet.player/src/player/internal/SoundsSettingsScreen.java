package player.internal;

import javafx.scene.Group;
import renderer.external.Renderer;

public class SoundsSettingsScreen extends Screen {

    public SoundsSettingsScreen(Group root, Renderer renderer) {
        super(root, renderer);
    }
    
}
